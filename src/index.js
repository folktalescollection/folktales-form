import React from 'react';
import ReactDOM from 'react-dom/client';
import FolktalesTree from './FolktalesTree';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <FolktalesTree />
  </React.StrictMode>
);
